// specialOffersComponent.js 自動產生

window.loadSpecialOffers = function () {
  const target = document.getElementById("specialOffersComponent");
  if (!target) return;

  target.innerHTML = `
    <section class="specialOffersComponent">
      <h2>loadSpecialOffers 區塊</h2>
      <p>這是 specialOffersComponent 的預設內容，可替換。</p>
    </section>
  `;
};